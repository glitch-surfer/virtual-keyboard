/* import generateKeyboard from './generate-keyboard.js';

const pressedShift = (event) => {
  if (event.code === 'ShiftLeft'
  || event.code === 'ShiftRight') {
    document.querySelector('.keyboard').remove();
    generateKeyboard();
  }
};
export default pressedShift; */
